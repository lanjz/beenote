import * as path from 'path';
export const VALIDA_ERR_MSG = '{PATH} = {VALUE} : Format error'
export const STATIC_PATH = path.join( process.cwd(), 'static' )
export const STATIC_IMG_PATH = path.join( STATIC_PATH, 'images' )
